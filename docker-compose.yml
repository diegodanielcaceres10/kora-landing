services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kora-roster-frontend
    ports:
      - "4002:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true
